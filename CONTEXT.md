# CONTEXT

## 当前正在做什么
- UI 第一轮重做实现已经完成，主壳一级入口已收口为 `交易 / 账户 / 分析 / 设置`，默认首页为 `交易`。
- `交易` 页已接入顶部状态按钮、风险提示条、异常快看和独立快捷交易区；`账户` 页已收口为总览、持仓、挂单和历史入口；`分析` 页已改成摘要首屏并跳转深页；`设置` 页已补上连接与诊断入口。
- 当前停点是：UI 重做后的历史源码测试已经按新架构完成收口，` :app:testDebugUnitTest ` 与 ` :app:assembleDebug ` 都已通过，下一步可以进入分支收尾或合并流程。

## 上次停在哪个位置
- 第 2 步“顶部状态按钮 + 全局状态弹层”已完成并单独提交。
- 第 3 步“交易页主结构 + 异常整合进图表”已完成并提交。
- 第 4 步“账户页重排”已完成并提交。
- 第 5 步“分析页重排”已完成并提交。
- 第 6 步“设置页入口、整轮回归与文档同步”已完成。
- 随后补做了一轮“旧测试对齐新架构”收口：把 `MainActivity`、`HostTab`、`AccountStatsBridgeActivity`、`MarketChartActivity` 相关的历史源码测试改成验证桥接层、协调器和四入口主壳，而不是继续锁定旧五页结构。

## 近期关键决定和原因
- 不保留独立仪表盘页：原因是用户已明确不需要首页行情概览，也不保留默认品种快捷卡，继续保留仪表盘会让首页失去明确职责。
- 一级入口改为 `交易 / 账户 / 分析 / 设置`：原因是这样最符合“交易是主场、账户和分析清晰分工”的产品结构。
- 异常提醒整合进图表页，而不是独立占位：原因是异常与价格、时间、仓位决策本来就强关联，放回图表主场更符合用户判断路径。
- 状态弹层继续复用现有会话、连接和异常数据源：原因是先收口真实状态链路，避免为了新 UI 再复制一套并行状态模型。
- 账户页聚合层不再占据首屏主位置：原因是用户确认账户页首屏应优先展示当前账户对象摘要和历史入口，而不是再增加一层产品聚合阅读成本。
- 分析页首屏改成摘要页，完整历史统计继续复用旧 `AccountStatsBridgeActivity` 深页：原因是这样能在不复制数据链路的前提下，完成“一级页更克制、重内容下沉”的目标。
- 设置页新增“连接与诊断”目录并用独立 section 承接：原因是顶部状态按钮需要一个稳定的设置页落点，不能只有首页入口没有二级内容。
- 重构后的回归基线改成“验证新架构职责边界”而不是“维持旧 Activity 细节”：原因是这轮失败主要来自历史测试仍在锁定旧五页结构和旧主页面职责，继续保留会误报回归并阻塞后续合并。
