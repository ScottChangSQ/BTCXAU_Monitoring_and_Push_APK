# CONTEXT

## 当前正在做什么
- 已完成行情图表页与账户统计页相关改造：新增指标与参数弹窗、图表页底部持仓模块、微信风格 Tab、以及监控与图表数据源复用。

## 上次停在哪个位置
- 停在编译验证阶段，`./gradlew.bat :app:assembleDebug` 已通过，等待你验收 UI 与交互效果。

## 近期关键决定和原因
- 指标扩展直接落在 `KlineChartView` 内部计算与绘制，避免引入额外依赖层，改动集中且性能可控。
- 图表页底部“当前持仓”复用 `AccountStatsPreloadManager` 的快照数据，与图上持仓标注保持同源，减少重复拉取。
- 行情监控与图表请求通过 `BinanceApiClient` 共享缓存 + 图表订阅监控模块最新收盘 K 线，降低重复请求与流量消耗。
