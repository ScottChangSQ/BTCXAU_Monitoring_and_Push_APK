# CONTEXT

## 当前正在做什么
- 正在按已批准的 UI 重做计划执行落地开发，主壳一级入口已经切换为 `交易 / 账户 / 分析 / 设置`，默认首页改为 `交易`。
- 交易页结构已完成一轮重做：顶部全局状态按钮、风险提示条、独立快捷交易区、当前品种摘要和异常快看底部弹层已经接入真实页面。
- 下一步进入第 4 步：重排 `账户` 页，让一级页只保留总览、持仓、挂单和历史入口的摘要层。

## 上次停在哪个位置
- 第 2 步“顶部状态按钮 + 全局状态弹层”已完成并单独提交。
- 第 3 步“交易页主结构 + 异常整合进图表”已经完成当前第一版，相关源码测试和布局资源测试已通过，准备继续做账户页。

## 近期关键决定和原因
- 不保留独立仪表盘页：原因是用户已明确不需要首页行情概览，也不保留默认品种快捷卡，继续保留仪表盘会让首页失去明确职责。
- 一级入口改为 `交易 / 账户 / 分析 / 设置`：原因是这样最符合“交易是主场、账户和分析清晰分工”的产品结构。
- 异常提醒整合进图表页，而不是独立占位：原因是异常与价格、时间、仓位决策本来就强关联，放回图表主场更符合用户判断路径。
- 状态弹层继续复用现有会话、连接和异常数据源：原因是先收口真实状态链路，避免为了新 UI 再复制一套并行状态模型。
- 风险提示条和异常摘要先走异常快看底部弹层：原因是这更符合已确认的“轻内容先弹层、重内容再进完整列表”规则。
